import 'package:flutter/material.dart';

class Numbers {
  static const double zero = 0;
  static const double two = 2;
  static const int three = 3;
  static const double four = 4;
  static const double ten = 10;
  static const double fifteen = 15;
  static const double twentyFive = 25;
  static const double fourty = 40;
  static const double fifty = 50;
  static const double hundred = 100;

  // Drawer
  static const double drawerWebWidth = 0.2;
  static const double drawerAppWidth = 0.6;

  // Drawer App Bar
  static const double drawerAppBarFontSize = 22;

  // Drawer Item
  static const EdgeInsets defaultLeftPadding = EdgeInsets.only(left: 8.0);

  // Contacts
  static const double deleteContactIconSize = 25;
  static const EdgeInsets contactsMargin = EdgeInsets.all(12.0);
  static const EdgeInsets deleteContactButtonPadding = EdgeInsets.all(1.0);

  // History
  static const double heartbeatRadius = 30;
  static const double heartbeatIconSize = 75;
  static const EdgeInsets measurementCardPadding = EdgeInsets.all(10);
  static const EdgeInsets measurementCardMargin =
      EdgeInsets.fromLTRB(20, 20, 20, 0);

  // Monitor
  static const double monitorWebWidth = 0.5;
  static const double monitorAppWidth = 1.0;
  static const int measurementDuration = 15;
  static const double gridViewAspectRatio = 1.5;
  static const double heartbeatsContainerHeight = 0.2;
  static const EdgeInsets monitorDefaultMargin = EdgeInsets.all(25);
  static const EdgeInsets heartbeatsContainerBottomMargin = EdgeInsets.only(bottom: 15);

  // Splash Screen
  static const heartIconSize = 90;
}
